# ============================================================================
# 環境變數範例檔案
# ============================================================================
# 使用方式：
# 1. 複製此檔案為 .env.dev：cp .env.example .env.dev
# 2. 填入實際的設定值
# 3. 不要將 .env.dev 提交到 Git（已在 .gitignore 中排除）
# ============================================================================

# ----------------------------------------------------------------------------
# 應用程式設定
# ----------------------------------------------------------------------------

# 環境名稱：local（本地開發）、dev（開發環境）、prod（生產環境）、cloudrun（gcp cloud run）
ENVIRONMENT=local

# 應用程式標題（顯示在 API 文件中）
APP_TITLE=花蓮光復救災平台 API

# 應用程式埠號（Docker 容器內使用）
PORT=8080

# PATCH and DELETE API KEY LIST
ALLOW_MODIFY_API_KEY_LIST=key1,key2,key3

# 生產環境伺服器 URL（預設值已在 config.py 中設定，可選）
# PROD_SERVER_URL=https://guangfu250923.pttapp.cc

# LAN SERVER URL (如果伺服器是架在 LAN 上的主機)
LAN_SERVER_UTL=http://192.168.1.107

# ----------------------------------------------------------------------------
# 資料庫設定（PostgreSQL）
# ----------------------------------------------------------------------------
# 提示：以下預設值可直接使用，適合本地開發和測試
# 生產環境請務必修改為強密碼

# PostgreSQL 容器使用的環境變數
POSTGRES_USER=guangfu_user
POSTGRES_PASSWORD=guangfu_dev_pass_2024
POSTGRES_DB=guangfu

# 後端應用程式使用的資料庫設定（保持與上方 POSTGRES_* 一致）
DB_USER=guangfu_user
DB_PASS=guangfu_dev_pass_2024
DB_NAME=guangfu

# 完整的資料庫連線 URL
# 格式：postgresql://<user>:<password>@<host>:<port>/<database>
#
# Docker Compose 環境（預設）：使用服務名稱 'postgres' 作為 host
DATABASE_URL=postgresql://guangfu_user:guangfu_dev_pass_2024@postgres:5432/guangfu

# ----------------------------------------------------------------------------
# LINE 登入設定（選用）
# ----------------------------------------------------------------------------
# 若需啟用 LINE 登入功能，請填入以下資訊
# 取得方式：https://developers.line.biz/ > 建立 LINE Login channel
#
# LINE Channel ID（從 LINE Developers Console 取得）
LINE_CLIENT_ID=""

# LINE Channel Secret（從 LINE Developers Console 取得）
LINE_CLIENT_SECRET=""

# ⚠️  DEPRECATED: LINE_REDIRECT_URI 已棄用
# 新的實作中，redirect_uri 由前端在呼叫 /line/authorize API 時提供
# 此設定保留以向下相容，但不再使用
LINE_REDIRECT_URI=""

# ----------------------------------------------------------------------------
# Discord Webhook 設定（選用）
# ----------------------------------------------------------------------------
# 若需啟用 Discord 通知功能，請填入 Webhook URL
# 取得方式：Discord 伺服器設定 > 整合 > Webhook > 建立 Webhook
#
# Discord Webhook URL（從 Discord 伺服器設定中取得）
DISCORD_WEBHOOK_URL=""
#
# 本地開發模式（僅啟動資料庫容器，後端在本機執行）：使用 'localhost' 作為 host
# DATABASE_URL=postgresql://guangfu_user:guangfu_dev_pass_2024@localhost:5432/guangfu

# ----------------------------------------------------------------------------
# GCP Cloud SQL 設定（僅供 dev/prod 環境使用）
# ----------------------------------------------------------------------------

# Cloud SQL Instance 連線名稱
# 格式：project-id:region:instance-name
# 範例：guangfu-project:asia-east1:guanfu-db-instance
# 本地開發可留空
INSTANCE_CONNECTION_NAME=

# ----------------------------------------------------------------------------
# 生產環境設定（選用）
# ----------------------------------------------------------------------------

# 測試/生產環境伺服器 URL（預設值已在 config.py 中設定，可選）
# PROD_SERVER_URL=https://api.gf250923.org
# DEV_SERVER_URL=https://uat-api.gf250923.org

# Discord Webhook URL
DISCORD_WEBHOOK_URL=https://discordapp.com/api/webhooks/1427363571112743177/KScjVsR03I07tROBByt655CnBCS15YlcmLxqg7Qu8Y27IaX-u1kdz7hKpVQv9Ljg0JtC
# ----------------------------------------------------------------------------